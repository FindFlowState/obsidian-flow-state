{
  "name": "flow-state-obsidian-plugin",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build-local": "node scripts/build.mjs --mode=local",
    "build": "node scripts/build.mjs --mode=prod",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/mustache": "^4.2.6",
    "@types/node": "^20.12.7",
    "esbuild": "^0.21.5",
    "obsidian": "^1.5.8",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@flowstate/supabase-types": "file:../../packages/supabase-types",
    "@supabase/supabase-js": "^2.45.0",
    "mustache": "^4.2.0",
    "sanitize-filename": "^1.6.3"
  }
}
